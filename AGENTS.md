# Client Agent Instructions

## 开始工作前

- 首先完整阅读 [`README.md`](./README.md)，理解项目功能和当前状态。
- 然后根据任务范围阅读相关的 `package.json`、构建配置和源码；不要只根据文件名或旧对话推断行为。
- 如果 README 与当前代码不一致，以代码和构建配置为事实来源，并在同一变更中更新 README，或在结果中明确说明差异。
- 修改前先检查工作区状态，不覆盖或回退用户已有的修改。

## 目录边界

```text
packages/
├── shared/    @any-draft/shared   领域类型、仓储接口与 ZIP 编解码
├── web/       @any-draft/web      Web 应用、编辑器、渲染与浏览器存储适配器
└── desktop/   @any-draft/desktop  Tauri 桌面端占位包，尚未实现
```

## 用户界面设计

- 编辑用户界面代码前，先检查附近的实现、可用的共享用户界面组件和现有的样式标记；说明可视化更改时将使用哪些共享组件或样式标记。
- 当代码更改涉及用户界面布局调整时，实施前使用类似 Wireframe 或 ASCIIFlow 风格的图表表达理解的布局结构，并与用户确认后再实施。
- 对于用户界面的更改，请首先选择 Tailwind 实用工具类。
- 使用共享设计标记，而不是硬编码颜色：
    - 在 Tailwind 类中，优先使用`border-border`、`bg-card`、`bg-background`、`bg-muted`、`text-card-foreground`、`text-muted-foreground`、`text-primary`和`text-destructive`等标记。
    - 在 Less/CSS 中，请使用 `var(--border)`、`var(--muted)`、`var(--muted-foreground)`、`var(--primary)` 和 `var(--destructive)`。
- 除非没有合适的标记，否则不要引入页面本地的十六进制、rgb 或 rgba 颜色。如果使用，请在最终响应中解释原因。
- 只有在选择器、响应边缘情况、伪元素或状态组合在 Tailwind 中不合适时，才使用自定义样式。
- 在创建自定义控件之前，重复使用`packages/web/src/components`基元。
- 如果有必要覆盖共享组件根类，应尽量减少覆盖并在最终回复中解释原因。

## React 文件拆分

- 一个 React 文件应围绕一个清晰的页面、功能或组件职责组织；避免让页面文件同时承载复杂数据请求、状态编排、业务转换和大量展示标记。
- 当组件包含多个相互独立的视觉区域、重复的 JSX、独立的交互状态，或已经难以快速定位修改位置时，应拆分为职责明确的子组件。
- 复杂状态、异步流程和可复用业务逻辑优先提取为同目录的自定义 Hook 或普通函数；纯展示组件只接收必要的 props，不直接依赖不相关的全局状态。
- 只在单一功能内使用的组件、Hook 和工具函数优先就近放在对应模块目录；跨功能复用且语义稳定后，再提升到共享目录。
- 组件 props 应保持最小且语义明确；不要为了避免拆分而传递整棵状态对象，也不要为了“通用”提前增加未使用的配置项。
- 拆分文件时同步移动相关类型、样式标记和测试，保留现有导入别名与命名约定；不要只按文件行数机械拆分。
- 避免过度拆分：简单、只使用一次且没有独立职责的 JSX 保持在当前文件中，防止组件层级和跳转成本无谓增加。

## 修改与验证

- 保持变更小而集中，沿用现有命名、别名、Prettier 和 workspace 结构；不要顺手重构无关代码。
- 代码变更至少执行：

    ```bash
    npm run check
    npm run format:check
    ```

- 依赖、运行时或构建配置变更还要执行 `npm run build`。
- markdown预览样式变更需要执行 `npm run test`。
- 新增或修改可测试业务逻辑时，同一变更应补充测试。

## 文档与状态

- 每次涉及功能变更时，检查 README 和相关文档是否需要同步。
